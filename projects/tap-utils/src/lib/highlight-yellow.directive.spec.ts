import { HighlightYellowDirectiveDirective } from './highlight-yellow-directive.directive';

describe('HighlightYellowDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightYellowDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
